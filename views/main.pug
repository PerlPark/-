doctype html
html
  include includes/head.pug
  body
  include includes/header.pug
  main
    form#form(action="/search" method="get")
      input(id="latInput" type="hidden" name="lat")
      input(id="lngInput" type="hidden" name="lng")
      input(id="routeIdInput" type="hidden" name="routeId")
      div#search-position
        a 현재 위치
        span 에서 
      div#search-bus
        a 30-2
        span 번 버스
      button(type="submit")
        i(class="material-icons") search
    a.button-style(href="/support/bus") 지원 버스 확인하기
    div#bookmark
      h2 저장한 버스 도착 정보
      p 저장된 버스 도착 정보가 없습니다. 자주 확인할 도착 정보를 저장하여 빠르게 조회하세요.
    script(src="/javascripts/getCurrentPosition.js")
    script(src="/javascripts/getBusList.js")
    script(src="/javascripts/main.js")